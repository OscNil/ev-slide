<% flash.each do | name, msg | %>
<%= content_tag :div, msg, :id => "flash" if msg.length > 0 %>
<% end %>

<!-- Check if there is a queue. TO BE IMPLEMENTED -->
<% if true %>
  <!-- If there is a queue... -->
  <% User.users_in_queue_in_order.each_with_index do | user, idx | %>
    <div class='ev-card d-flex justify-content-between<% if user.id == current_user.id %><%= " me-card" %><% if @available_posts.count > idx %><%= " me-card-charger-available" %><% end %><% end %>'>
      <div><%= image_tag user.photo, class: "avatar" %></div>
      <div class="d-flex justify-content-between list-for-name">
        <div><%= user.first_name %> <%= user.last_name %></div>
        <div>
          <!-- IF there ARE available charging posts... -->
        <% if @available_posts.count > idx %>
          <!-- ...calculate the time remaining until scheduled checkout... -->
          <%= image_tag "charger.png", id: "q-charger" %> <%= @available_posts[idx].id %>
        <% else %>
          <!-- ...ELSE just put it to zero -->
          time remaining
          <% if user.id == current_user.id %>

          <% else %>

          <% end %>
        <% end %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>

